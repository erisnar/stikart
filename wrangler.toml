name = "stikart-submit"
main = "worker.js"
compatibility_date = "2024-09-23"

# Deploy:  wrangler deploy
# Secret:  wrangler secret put GITHUB_TOKEN
#          (fine-grained PAT: Contents write + Pull requests write on erisnar/stikart)
#
# Optional env var to restrict allowed origins (default: stikart.no + localhost:8000):
# wrangler secret put ALLOWED_ORIGINS
